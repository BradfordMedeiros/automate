
var message_processor = {
	name: "message_processor',

	functions: {
		handle
	}
	

}

module.exports = networkInterface;


// should define the methods network components will be using
// 

// this is kind of a scratch file atm
/* var abstractNetwork = func
	[]
	-----	
	add(Bluetooth);
	add(Wifi);
	etc
	etc
}

// getSupportedNetworkTypes --> returns wifi, bluetooth, etc

sendOverNetwork (message, config ) =>
// getNetworkId  (returns something like BT or WIFI whatever)
// if (message

// you decide which network module to send on based on the type,
// which corresponds to the field of getNetworkID

*/

/*
	config: 
	    
	    device: {
		identifier,
		ip,
		type: <bluetooth, wifi, etc>
	    } 

	    subscriptions: [] 

	}
*/
