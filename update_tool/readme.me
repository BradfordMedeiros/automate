idea is that this will be a simple script which downloads new code from a known source as a .tar file.  It will then tar the old code and save it as .old.  

Then it will untar the new code and specify where main is.

Have the script check for update.  Have as a cronjob
