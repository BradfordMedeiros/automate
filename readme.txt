This folder are resources that are used to validate that previous output matches current output.  

The first time the test was run it creates it.  After it creates it, but does not save it, and checks if the output still matches.